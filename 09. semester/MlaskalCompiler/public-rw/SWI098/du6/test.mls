program du4test1;
{label 1;}
var b:boolean;
i:integer;
procedure inc(var i:integer);
begin
	i := i+1;
end;
procedure inc2(var i:integer);
begin
	inc(i);
	inc(i);
end;
begin
	
	{b := true;
	(true > false) and 
		('A' <= 'B') or 
		(1.0 <> 0);}
	{if (1 + 0 = 1) then
		writes('true');}

	{i:=4;
	while (i > 0) do
	begin
		writei(i);
		i:= i - 1;
	end;}
	{repeat
		writei(i);
		i:= i - 1
	until i > 0;}
	{for i:= 1 to 0 do
		writei(i);}

	i:=1;
	inc2(i + 1);
	writei(i);

end.